Primeiramente, precisa ajustar a variável global "arq_origem" localizada na linha 10 do arquivo 
"main.cu" na pasta src/, subsituindo pelo path de acordo com sua máquina.

Para compilar o programa, utilizar o makefile
Execute o executavel utilizando ./parell